setLogoProperties = (logoText, shape, shapeColor) => {
  const logo = {
    logoText: logoText,
    shape: shape,
    shapeColor: shapeColor
    };

  return logo;
}

module.exports = setLogoProperties;