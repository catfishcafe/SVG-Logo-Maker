setLogoProperties = (logoText, textColor, shape, shapeColor) => {
  const logo = {
    logoText: logoText,
    textColor: textColor, 
    shape: shape,
    shapeColor: shapeColor
    };

  return logo;
}

module.exports = setLogoProperties;